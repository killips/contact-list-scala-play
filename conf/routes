# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.PhoneListController.index

POST    /add-contact                controllers.PhoneListController.addContact

POST    /delet-contact              controllers.PhoneListController.deletContact

POST    /search-contact             controllers.PhoneListController.searchContact

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)